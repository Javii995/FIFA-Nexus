import app from './app';

// De app wordt al opgestart in app.ts na MongoDB connectie
// Dit bestand is alleen nodig voor de startpunt van de applicatie

// Als je TypeScript compileert met tsconfig.json, zal dit het hoofdbestand zijn dat wordt gestart